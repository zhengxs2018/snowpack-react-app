export enum AuthStatus {
  LOADING = 'loading',
  CONNECTED = 'connected',
  NOT_AUTHORIZED = 'not_authorized',
  UNKNOWN = 'unknown',
}

export enum UserStatus {
  LOADING = 'loading',
  CONNECTED = 'connected',
  UNKNOWN = 'unknown',
  ERROR = 'error',
}
